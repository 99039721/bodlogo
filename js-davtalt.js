// const arr = (arr) => {
//   arr.forEach((element) => {
//     console.log(element);
//   });
// };

// arr([1, 123456, 34]);

const printArray = (arr) => {
  arr.forEach((element) => {
    console.log(element);
  });
};

printArray([1, 2, 3, 4, 5]);
